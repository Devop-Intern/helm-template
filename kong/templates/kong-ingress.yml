apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ .Values.ingress.kong_ingress }}
  annotations:
    kubernetes.io/ingress.class: "kong"
spec:
  rules:
    - host: {{ .Values.ingress.host }}
      http:
        paths:
          - path: {{ .Values.echoserver.kong_path }}
            pathType: {{ .Values.echoserver.pathType }}
            backend:
              service:
                name: {{ .Values.echoserver.kong_name }}-svc
                port: 
                  number: {{ .Values.echoserver.servicePort }}

          - path: {{ .Values.helloserver.kong_path }}
            pathType: {{ .Values.helloserver.pathType }}
            backend:
              service:
                name: {{ .Values.helloserver.kong_name }}-svc
                port: 
                  number: {{ .Values.helloserver.servicePort }}
                  