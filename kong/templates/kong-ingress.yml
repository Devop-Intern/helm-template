apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ .Values.ingress.kong_ingress }}
  annotations:
    kubernetes.io/ingress.class: "kong"
spec:
  rules:
    - host: {{ .Values.ingress.host }}
      http:
        paths:
          - path: {{ .Values.echoserver.kong_path }}
            pathType: {{ .Values.echoserver.pathType }}
            backend:
              service:
                name: {{ .Values.echoserver.name }}-svc
                port: 
                  number: {{ .Values.echoserver.servicePort }}

          - path: {{ .Values.hellserver.kong_path }}
            pathType: {{ .Values.hellserver.pathType }}
            backend:
              service:
                name: {{ .Values.hellserver.name }}-svc
                port: 
                  number: {{ .Values.hellserver.servicePort }}
                  