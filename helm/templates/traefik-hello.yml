apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.helloserver.traefik_name }}
  labels:
    app: {{ .Values.helloserver.traefik_name }}
spec:
  replicas: {{ .Values.helloserver.replicas }}
  selector:
    matchLabels:
      app: {{ .Values.helloserver.traefik_name }}
  template:
    metadata:
      labels:
        app: {{ .Values.helloserver.traefik_name }}
    spec:
      containers:
      - name: {{ .Values.helloserver.traefik_name }}
        image: {{ .Values.helloserver.image }}
        ports:
        - containerPort: {{ .Values.helloserver.containerPort }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.helloserver.traefik_name }}-svc
spec:
  type: {{ .Values.helloserver.serviceType }}
  ports:
  - port: {{ .Values.helloserver.servicePort }}
  selector:
    app: {{ .Values.helloserver.traefik_name }}
