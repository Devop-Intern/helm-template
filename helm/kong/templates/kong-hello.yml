apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.helloserver.kongname }}
  labels:
    app: {{ .Values.helloserver.kongname }}
spec:
  replicas: {{ .Values.helloserver.replicas }}
  selector:
    matchLabels:
      app: {{ .Values.helloserver.kongname }}
  template:
    metadata:
      labels:
        app: {{ .Values.helloserver.kongname }}
    spec:
      containers:
      - name: {{ .Values.helloserver.kongname }}
        image: {{ .Values.helloserver.image }}
        ports:
        - containerPort: {{ .Values.helloserver.containerPort }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.helloserver.kongname }}-svc
spec:
  type: {{ .Values.helloserver.serviceType }}
  ports:
  - port: {{ .Values.helloserver.servicePort }}
  selector:
    app: {{ .Values.helloserver.kongname }}
